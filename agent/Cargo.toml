[package]
name = "pulse-agent"
version = "0.1.0"
edition = "2024"

[dependencies]
tokio = { version = "1", features = ["full"] }
dotenvy = "0.15"
tokio-util = { version = "0.7", features = ["rt"] }
reqwest = { version = "0.13.2", features = ["json"] }